using System;

namespace CSharp_Livia
{
    class ViewActivityImpl : ViewActivity
    {
        private const string name;
        private const Optional<int> capacity;
        private const Optional<int> minPrice;
        private const Optional<int> maxPrice;
        private const enum ActivityType{FAIR, BABYFAIR, REST, SHOP};
        private const ActivityType activityType;

        
    }
}